int tab[5] = {2,8,4,32,16};

&var = l'adresse de var
*pointeurVar = contenu de l'adresse var
int * ptrTab;
ptdrTab = tab; //recoit l'adresse du premier élèment du tableau


-Allocation dynamique de la mémoire(en C) via malloc

int * ptr = malloc(10*sizeof(int)); // Si échec alors maloc retourne NULL (sinon il va pointer sur le premier élèment du tableau)

--> Allouer tab taille de 10 entiers (en C++ c'est new et pour détruire delete ou delete[])

--> free (en C et non delete)


