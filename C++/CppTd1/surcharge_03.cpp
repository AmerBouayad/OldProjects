/*!
 * \file surcharge_03.cpp
 * \brief surcharge de fonctions
 */
#include <iostream>
/*
void f(short) {
    std::cout << "f(short)" << std::endl;
}

void f(double) {
    std::cout << "f(double)" << std::endl;
}

int main() {
    //f(3);            // erreur de compilation

    f(4.5);            // affiche f(double)

    //f(true);         // erreur de compilation

    //f(3LL);          // erreur de compilation

    //f('T');          // erreur de compilation

    f(.3F);            // //affiche f(double)

    //f(5U);           // erreur de compilation

    short s {44};
    f(s);              // affiche f(short)

    //f(2e-2L);        // erreur de compilation
}

*/
