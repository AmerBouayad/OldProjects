/*!
 * \file surcharge_04.cpp
 * \brief surcharge de fonctions
 */
#include <iostream>
/*
void f(int) {
    std::cout << "f(int)" << std::endl;
}

void f(unsigned) {
    std::cout << "f(unsigned)" << std::endl;
}

void f(long long) {
    std::cout << "f(long long)" << std::endl;
}

void f(double) {
    std::cout << "f(double)" << std::endl;
}

void f(long double) {
    std::cout << "f(long double)" << std::endl;
}

int main() {
    f(3);         // affiche f(int)
    f(4.5);       // affiche f(double)
    f(true);      // affiche f(int)
    f(3LL);       // affiche f(long long)
    f('T');       // affiche f(int)
    f(.3F);       // affiche f(double)
    f(5U);        // affiche f(unsigned)
    short s {44};
    f(s);         // affiche f(int)
    f(2e-2L);     // affiche f(long double)
}
*/
