/*!
 * \file surcharge_02.cpp
 * \brief surcharge de fonctions
 */
#include <iostream>
/*
void f(int) {
    std::cout << "f(int)" << std::endl;
}

void f(long double) {
    std::cout << "f(long double)" << std::endl;
}

int main() {
    f(3);        // affiche f(int)

    //f(4.5);    // erreur de compilation

    f(true);     // affiche f(int)

    //f(3LL);    // erreur de compilation

    f('T');      // affiche f(int)

    //f(.3F);    // erreur de compilation

    //f(5U);     // erreur de compilation

    short s {44};
    f(s);        // affiche f(int)

    f(2e-2L);    // affiche f(long double)
}

*/
